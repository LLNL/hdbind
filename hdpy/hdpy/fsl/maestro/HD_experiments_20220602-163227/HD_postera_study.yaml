description:
  name: HD_experiments
  description: run HDC experiments on various configurations.


batch:
  type: slurm
  # queue: pbatch
  queue: pvis
  host: pascal
  bank: ncov2019
  qos: expedite

study:
  - name: postera
    description: run eval job on configuration
    run:
      cmd: |

            cd /usr/WS1/jones289/hd-cuda-master/hdpy/hdpy/fsl

            DATA_DIR=datasets/dude/deepchem_feats/$(TARGET)/$(FEAT)
            python experiment_single_classifier.py --dataset postera --out_csv results/postera/$(TARGET)_$(FEAT)_test_results.csv --n_problems 30 --support_path ${DATA_DIR}/train.npy --query_path ${DATA_DIR}/test.npy --kquery -1


      nodes: 1
      procs: 9
      # walltime: "1-00:00:00"
      walltime: "12:00:00"